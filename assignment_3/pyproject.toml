[project]
name = "assignment-3"
version = "0.1.0"
description = "Week 3 Assignment 2: Containerize Stack with Docker and PostgreSQL"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "uvicorn>=0.30.0",
    "psycopg2-binary>=2.9.9",
    "python-dotenv>=1.0.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]
