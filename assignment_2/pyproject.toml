[project]
name = "assignment-2"
version = "0.1.0"
description = "Week 3 Assignment 1: SQLite Database CRUD API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "uvicorn>=0.30.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]
